# Data

This project fetches historical BTC-USD prices using the `yfinance` library during runtime.

If you prefer to run fully offline, you can export the downloaded data to a CSV and load it from `data/`.
